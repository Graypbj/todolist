# Web frontend

This directory is reserved for a lightweight frontend (HTML/CSS + JavaScript or TypeScript).

Recommended starting options:
- Plain HTML/CSS/JS with a tiny build step later if needed
- TypeScript + a minimal bundler if desired (optional)

During development you can run the API separately and serve these files using any static server.
